import { LyricScraperPipe } from './lyric-scraper.pipe';

describe('LyricScraperPipe', () => {
  it('create an instance', () => {
    const pipe = new LyricScraperPipe();
    expect(pipe).toBeTruthy();
  });
});
